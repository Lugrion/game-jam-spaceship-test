export type SpriteBasicConfig = {
    current_scene: Phaser.Scene,
    x: number,
    y: number,
    texture: string | Phaser.Textures.Texture
}

export type PlanetType = "tubelectric"

export type SpaceshipType = "squarimus" | "circulus"

export type WeaponType = "vulcan" | "laser" | "uzi"
